import * as React from 'react'

const GalleryAssetsManagerCell = ({ data }) => {
  if (!data) {
    return null
  }

  return <span>Upload queue ready</span>
}

export default GalleryAssetsManagerCell
